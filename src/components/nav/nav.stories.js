import React from 'react';
import { Button } from '@storybook/react/demo';

export default { title: 'Nav' };

export const nav = () => {
  return (
    <nav>
      <span>Hello there</span>
    </nav>
  );
};

export const navWithProps = () => {
  return (
    <nav>
      <span>Hello there</span>
    </nav>
  );
};
