import React from 'react';
import PropTypes from 'prop-types';

const nav = props => {
  return (
    <nav>
      <span>Hello there</span>
    </nav>
  );
};

nav.propTypes = {};

export default nav;
